version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: Running Tests
          command: |
            cd integration-tests
            npm run test
      - store_test_results:
          path: integration-tests/test-results
